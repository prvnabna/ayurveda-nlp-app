# Ayurveda NLP Pipeline — Streamlit App
# Install: pip install -r requirements.txt

# ── Streamlit UI ──────────────────────────────────────────────
streamlit>=1.35.0

# ── numpy MUST be pinned to <2.0 — spaCy/thinc wheels are compiled
#    against numpy 1.x; numpy 2.x causes binary incompatibility on
#    Streamlit Cloud (ValueError: numpy.dtype size changed)
numpy>=1.24.0,<2.0.0

# ── Core NLP — spacy must stay <3.8.0 for xx-ent-wiki-sm 3.7.0 ──
spacy>=3.7.0,<3.8.0
https://github.com/explosion/spacy-models/releases/download/xx_ent_wiki_sm-3.7.0/xx_ent_wiki_sm-3.7.0-py3-none-any.whl

# ── Document reading (PDF + DOCX) ────────────────────────────
pymupdf>=1.24.0
python-docx>=1.1.0

# ── QA / OCR accuracy ────────────────────────────────────────
jiwer>=3.0.0

# ── Utilities ─────────────────────────────────────────────────
regex>=2024.0.0
tqdm>=4.66.0

# ── Data / output formats ─────────────────────────────────────
pandas>=2.0.0,<3.0.0
openpyxl>=3.1.0